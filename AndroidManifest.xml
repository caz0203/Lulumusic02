<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 触感反馈（ui/components/BeansHaptics.kt） -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
      桌面歌词悬浮窗（playback/DesktopLyricsOverlay.kt）。
      这是特殊权限：声明之后系统仍会默认拒绝，必须由用户在
      「设置 → 显示在其他应用上层」里逐应用授予（设置页会检测
      Settings.canDrawOverlays 并把用户送过去；没授权时功能会明确报出原因，不会静默失败）。
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Local audio library + offline downloads -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!--
      问题反馈走 mailto: 邮件（data/feedback/EmailFeedback.kt）。
      Android 11+ 的包可见性过滤会让 resolveActivity 看不到邮件 App，从而误判成
      「没有邮件 App」并直接退化成复制到剪贴板，所以这里必须声明这条 queries。
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:name=".BeansApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.LuluMusic"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|smallestScreenSize"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LuluMusic">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background playback + lock-screen / Bluetooth controls -->
        <service
            android:name=".playback.BeansPlayerService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!--
          设置页「备份与恢复」导出后走系统分享面板保存备份文件。
          只开放应用缓存目录（res/xml/file_paths.xml），authority 见 BackupManager.AUTHORITY。
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.lulu.music.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
